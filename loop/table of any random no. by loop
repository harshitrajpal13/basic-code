#include <stdio.h>

int main()
{
   int i, num;
   
    printf("Enter number :\n \n");
    
   for(i>=1; i<=10; i++)
   {
      printf("%d * %d = %d\n\n",  i, num ,(num*i));
      scanf("%d",&num);
   }
 
   
   return 0;
}
