#include<stdio.h>

int main()
{
    int a, b, c, x;

    printf("Enter the value of a: ");
    scanf("%d", &a);

    printf("Enter the value of b: ");
    scanf("%d", &b);
    
 Ll
    c = 1;
    x = b;
    while(b!=0)
    {
       
    }
   
}
