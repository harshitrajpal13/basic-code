#include <stdio.h>

int main()
{
    for(int i=0; i<9; i++){
        for(int j=0;j<9;j++){
            if(i+j>=9)   
                printf(" %d%d ",i,j);
            else
                printf("    ");
        }
        for(int k=1;k<i;k++){
                printf(" %d%d ",i,k);
        }
        printf("\n");
    }

    return 0;
}
